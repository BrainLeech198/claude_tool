# 0.4 设计：界面重构 + 插件宿主

这一版干两件事，而且必须一起干：**把主窗重做成"挑一本书、开 Claude"**，同时
**把 claude_tool 从启动器升级成插件宿主**。

为什么必须一起：插件要往界面上挂东西（顶栏按钮、工作区行操作、右栏详情）。如果先
给现在这套界面挂一遍、再重做界面，等于同样的活干两遍。挂载点本来就该长在新布局里。

---

## 一、主窗重做成左导航 + 右详情

### 现在的问题

`_build_ui():396` 把四件事平铺在一屏里：

- 顶栏：当前模型胶囊、claude 版本、「查更新」、「打开配置目录」（`:400-431`）
- 「模型」区：右上 `[+ 添加][导入当前][测试][刷新]`，底下空状态引导或列表
- 「工作区」区：右上 `[+ 添加工作区][重新扫描]`，底下 AI 托管、默认工作区、筛选框、列表
- 底栏：四个勾（`:471-490`）+ 两行注解 + 状态行

编辑类操作和"日常操作"混在一起，两块列表上下平铺。结果是窗口又高又杂，而它日常
只需要干一件事：**选一个目录，开 claude**。

### 新形态

```
┌ Claude 启动器 ───────────────────────────────── □ ✕ ┐
│ ┌────────────┐ ┌───────────────────────────────────┐ │
│ │ 工作区      │ │ 雾中点名                           │ │
│ │ ▸ 雾中点名   │ │ D:\File\claude\type\雾中点名        │ │
│ │   社畜魂穿   │ │                                   │ │
│ │   旧稿       │ │ claude 2.1.150 · DeepSeek          │ │
│ │             │ │                                   │ │
│ │             │ │ [新会话] [接着上次] [打开目录]      │ │
│ │             │ │                                   │ │
│ │ ─────────── │ │ ── 插件 ──                         │ │
│ │  ⚙ 设置     │ │ 用小说工作台打开                    │ │
│ │  ⊞ 插件     │ │                                   │ │
│ └────────────┘ └───────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

- **左栏**：工作区列表（就是现在那个列表，搬到左边竖着摆）+ 筛选框 + 底部的
  设置/插件入口。
- **右栏**：选中那本的详情 —— 名字、路径、本机 claude 版本和当前模型、三个动作
  按钮，底下是插件挂载区。
- **顶栏**：缩到一行，只留「有新版」这类**需要立刻知道**的提示。模型名和版本号
  挪进右栏详情（它们是"选中这本书时"的信息，不是全局信息）。
- **底栏**：四个勾全搬进设置窗（见二）。状态行留着 —— 它是反馈的唯一出口。

### 「当前工作区」：新概念

现在没有这个概念。点一下工作区行就是直接 `launch_workspace():1807` 开会话，
中间没有"选中"这一步。插件需要知道"用户在盯哪本书"，所以新增：

- 左栏单击 = **选中**（高亮那行），写进 `launcher.json` 的 `selected_workspace`
  （按路径记，工作区改名不影响）。
- 右栏那三个按钮作用于选中的那本。
- `Autonomy`（AI 托管）和默认工作区那两行挪进设置窗的「工作区」分页。
- 启动时：`selected_workspace` 有值且目录还在 → 选中它；否则选第一条。

**注意这改了现有交互**：单击不再直接开会话，要点右栏的「新会话」。行首那个
"按住 Ctrl 直接开"的快路（`_build_workspace_rows` 一带）保留 —— 熟手的手感别动。

---

## 二、设置窗（一个窗 + 左侧分页）

入口只有两个：左栏底部「⚙ 设置」、右栏（或顶栏）没有重复入口。窗口内左侧分页：

| 分页 | 装什么 |
|---|---|
| 模型 | 现在「模型」区那一整套：列表 + `[+ 添加][导入当前][测试][刷新]` |
| 工作区 | AI 托管、默认工作区、`[+ 添加工作区][重新扫描]`、搬迁/改名/移除 |
| 行为开关 | 内嵌终端 conhost、自动继续 Stop hook、自动查 claude 新版（三个勾） |
| 关于 | 版本号、官网链接、查启动器新版（现在那个「有新版」的手动入口） |

一个窗而不是每域一个窗：入口唯一，以后加设置项不用再加窗口 —— 现在这个"凌乱"
的观感，恰恰是入口太多造成的。

**连锁改动**：`_apply_min_size` 现在是拿底栏那排勾的**自然宽度**当窗口下限基准的
（`:440` 注释、`:461-463`、`:493-494` 两处都写明"挤成一行会把下限顶宽一大截"）。
勾搬进设置窗之后，主窗下限的基准要重新挑一个（建议按左栏最小宽 + 右栏动作按钮
那一排量），这段逻辑得整段重算，不能只删勾。

**实际（Task 6–7 已落地）**：下限改按 `NAV_WIDTH + PAGE_PAD + DETAIL_MIN_WIDTH`
算，落地是 **726×660**（0.4 之前是 713×660）。有意思的是那几个勾从来就不是瓶颈
——它自然宽 552 掉到 330，下限纹丝不动；真正管着下限的是左列那 633 像素。所以
"搬走开关，下限会跟着掉"这个直觉是错的，别再照着它去调 `MIN_MARGIN`。

**再往下（Task 8 + 0.4 收尾后是 804×660）**：`DETAIL_MIN_WIDTH` 按实测从 390 提到
460（+70），正文两侧又统一成 `PAGE_GUTTER`（14 → 20，+12）。见九那节。

---

## 三、拆 `ui/launcher.py`

2292 行单文件，插件挂载点、"当前工作区"状态、设置窗都会往它里面堆。按域拆：

```
ui/
  launcher.py    主窗骨架：装配、窗口尺寸、事件循环。目标 400 行以内
  nav.py         左栏：工作区列表、筛选、选中态、右键菜单
  detail.py      右栏：选中详情、动作按钮、插件挂载区
  settings.py    设置窗 + 四个分页
  plugins_ui.py  插件管理窗
  dialogs.py     现有那些对话框（不动）
```

现有的 `_build_section()`/`_build_workspace_rows()`/`_build_terminal()` 等按域归位。
拆的依据是**探针**：它们按名字找控件、按 `type(child).__name__` 找 `PillButton`
（见 `_probe_ui_shot.py:194` 的 `find_pill`），拆分不改控件类名和可见文案，
探针就还认得出。所以**重构期间控件的类型和文案尽量别动，只动装配位置**。

### 实际拆成了什么样（0.4 做的时候对了一遍）

上面那张图是没动手时的草图。真按方法边界切下来，粒度比它细——`launcher.py`
里本来就有按域分好的注释节，照着节切最省事，"一域一个 mixin"比"左栏/右栏"这种
按控件的分法更贴代码原来的组织方式。落地是这样：

```
ui/
  launcher.py     主窗骨架：__init__、窗口尺寸/位置、_build_ui 的装配、底栏、
                  两栏装配顺序，以及不属于任何一域的几个小方法。652 行
  nav.py          左栏导航（Task 6）          detail.py   右栏详情（Task 6）
  settings.py     设置窗 + 四个分页（Task 6–7）  plugins_ui.py 插件管理窗（Plan 2）
  dialogs.py      现有那些对话框（不动）
  update.py       顶栏版本检查 + 启动器自更新
  models.py       模型列表、测试、换模型那条流水线
  workspaces.py   工作区列表、搬迁改名、默认目录
  sessions.py     开出去的会话、「正在跑」、交接文档
  terminal.py     内嵌终端（只在 Windows 上有）
```

`Launcher` 的多继承列表：`UpdateMixin, ModelsMixin, WorkspacesMixin, SessionsMixin,
TerminalMixin, LauncherDialogs, tk.Tk`。实例状态一个不动，全留在 `launcher.py` 的
`__init__` 里——mixin 里照旧自由用 `self`。

拆的过程里冒出来三件"只动代码也躲不开"的事，记在这儿免得下次再踩：

1. **共享常量得有个公共的落脚处。** `SIDE_GAP`（左列和终端栏之间那个空档）拆完
   之后三处要用：主窗、`sessions.launch_workspace`、`terminal._room_for_terminal`。
   后两者 `import` 不到 `launcher` 的模块级常量（会绕成循环 import），所以它挪进
   了 `theme.py`——那儿本来就是跨模块的界面常量集散地。
2. **探针的猴补丁会静默落空。** 方法一搬走，`L.xxx = 假实现` 那种打补丁就不灵了，
   而且**不报错**。用 `_probe_common.rebind()` 代替；每切一刀都要照
   `temp/probes/BASELINE.md` 第 4.2 节那两个 `grep` 重审一遍。
3. **后台跑 GUI 探针的那个启动器不能提前 `import claude_tool.*`。** `paths.py` 是
   在它自己 import 那一下把 `~` 展开成常量的，提前 import 就把真实家目录冻住了、
   探针的沙箱失效（探针会去动用户真身上的配置）。详见 `_probe_run.py` 文件头。

---

## 四、启动器自更新：去掉勾，改自动

删掉 `auto_self_var`（`:455` 定义、`:482` 那个勾、`_on_self_check_toggle` 回调），
改成启动时自动查一次启动器新版。

**这是一次立场变更，得写进文档**：现在这套是刻意让联网默认关的 ——
`设计说明.md:498`「联网这件事默认是关的」、`config.py:37` 的注释「自动查更新会
联网，都不该悄悄替」。去掉勾等于启动即联网。

- `config.py` 里 `auto_self_update` 这个键失去意义，读旧配置时忽略即可（不删键，
  免得老配置文件白跑一次迁移）。
- 旁边「自动查 claude 新版」那条勾**不动**，它管的是另一件事。
- 同步改：`README.md:52/55`、`设计说明.md:498/560`，两处口径要跟新行为一致。

---

## 五、插件宿主

### 5.1 目录与清单

```
claude_tool/plugins/          ← 每个子目录一个插件，各自独立版本管理
  novel_assistant/
    plugin.json               ← 不执行代码就能读的清单
    __init__.py               ← entry 指到这
```

`plugin.json`：

```json
{
  "id": "novel_assistant",
  "name": "小说工作台",
  "version": "0.1.0",
  "api_version": 1,
  "min_host": "0.4.0",
  "author": "Mr_brainleech",
  "description": "按作品集结构看设定、切章节、审 Claude 的改稿提案",
  "entry": "__init__.py:register"
}
```

**清单必须是独立文件、不能写在 Python 模块里**：信任确认要在**加载之前**把
名称/作者/版本/路径摆给用户看，那就不能先 import 它。

### 5.2 发现与加载

1. 扫 `plugins/` 下每个子目录，读 `plugin.json`。读不到或字段缺 → 记一条"坏插件"，
   在插件管理里照实说，不抛异常。
2. 查 `~/.claude_tool/plugins.json` 里的启用状态。**首次见到的插件 = 未启用**。
3. 校验 `api_version` == 宿主当前值、`min_host` <= 宿主版本。任一不过 → 灰着，
   写明原因，不加载。
4. 未启用/校验不过的一律不 import。用户在插件管理窗点「启用」才 import。
5. `entry` 指向的函数收到一个 `host` 对象，就是它的全部世界。

### 5.3 信任模型

同进程 = 插件在宿主进程里跑任意代码，**这不是沙箱**。所以：

- 首次加载**必须**用户点「启用」（插件管理窗里列出名称/作者/版本/路径）。
- 启用状态记进 `~/.claude_tool/plugins.json`，之后不再问。
- 插件升级（`version` 变了）时**重新问一次** —— 不然一个插件第一次被审过之后，
  后续版本就能为所欲为。
- 插件目录里的 `plugin.json` 和代码是分开的，包可能来自别人，文档里明说这一点。

### 5.4 宿主 API（v1 就这么大）

```python
host.version                    # "0.4.0"
host.api_version                # 1
host.workspaces()               # 工作区列表，转发 config.load_config()
host.current_workspace()        # 左栏选中那条的 dict，没有则 None
host.on_workspace_change(cb)    # 订阅切换，cb(entry_or_None)
host.open_claude(path, prompt=None, cont=False, permission=None)
                                # 转发 winhost.spawn_terminal（winhost.py:114）
host.claude_args(...)           # 把 claude.py:20 那几个函数直接露出来
host.register_action(where, text, callback)
host.register_settings_page(title, build)
host.log(text)                  # 写状态行
host.theme / host.widgets       # 模块本身
```

刻意**不**暴露：`Launcher` 实例、`config_data`、任何 `_` 开头的私有物。插件要的
是能力，不是宿主的内部状态。

### 5.5 挂载点

| where | 位置 | 典型用法 |
|---|---|---|
| `toolbar` | 主窗顶栏右侧 | 插件的全局入口 |
| `workspace_detail` | 右栏详情区 | 「用小说工作台打开」（拿到选中那本） |
| `workspace_row` | 左栏列表行右键菜单 | 针对某一本，不一定先选中 |

顺带把顶栏那两颗硬编码按钮（`:425` 查更新、`:407` 打开配置目录）改成数据驱动 ——
`_build_section(..., actions=[...]):616` 已经是这个形状了，照它做。

### 5.6 版本门禁

- `api_version`：整数，**接口破坏性变更才加**。宿主当前 = 1。
- `min_host`：只设下限（不做完整范围表达式，v1 用不上）。
- 预留：解析里留一个"网上仓库清单"的字段（将来像 `docs/releases.js` 那样从网上
  拉插件列表）。**v1 只留字段，不实现拉取。**

### 5.7 打包

- `build/claude_tool.spec:33` 的 `datas` 加 `('../claude_tool/plugins', 'plugins')`。
- 运行时目录常量照 `paths.ICON_FILE`（`paths.py:50`）那套 `_MEIPASS` 双路走。
- 加载用 `importlib.util.spec_from_file_location`，把插件目录加进 `sys.path`。
- **不要**把插件塞进 `hiddenimports`（`spec:37`）—— 那是手工维护的清单，加个插件
  就得改一次 spec。插件当 `datas` 收源码、运行时加载。
- `build/claude_tool.iss:56` 用 `recursesubdirs` 递归收 `dist/`，不用改。

### 5.8 v1 明确不做

- **插件不能挂自己的 hook**。`handoff.hook_settings_payload():565` 是单占的，要支持
  多方挂载得先造合并机制，而 novel_assistant 用不到它。留到 `api_version` 2。
- **插件不能往宿主窗口里嵌面板**。同进程，但界面自开 `Toplevel` —— 不跟这套布局
  打架，也不用给宿主造布局契约。
- **不实现网上下拉插件仓库**，只留字段。

---

## 六、novel_assistant（接口落地后再细化）

已定的：三区单窗口（左导航 / 中正文 / 右提案面板）、阅读视图 + 审阅视图可切、
设定文档查看与编辑、顶部工具栏切章节、提案文件 mtime 轮询、唤起 Claude 走
`host.open_claude(cont=True, prompt=...)`。

未定的（等下 5.4 那套 API 真跑起来再定，现在是纸上的）：三区具体尺寸、正文锚定的
匹配精度、选择题往返的解析边界。

**编码约束照旧**：作品集里全是 UTF-8 with BOM，写文件必须保留 BOM。

---

## 七、验收：拿探针当回归网

根目录 40 多个 `_probe_*.py` 是现成的回归网。顺序：

1. **动界面之前**先跑一遍能跑的，把输出存成基线。
2. 重构完再跑一遍，逐条对比。

重点：

| 探针 | 管什么 |
|---|---|
| `_probe_ui_shot.py` | 主窗截图、空状态、悬停提示（重构后图必然变，看的是"没崩、该有的还在"） |
| `_probe_install.py` / `_probe_instguard.py` | 装 claude 那条路和护栏（这条刚修好，别再碰坏） |
| `_probe_hookcmd.py` / `_probe_askhook.py` | hook 命令行拼装 |
| `_probe_version.py` / `_probe_selfupdate.py` | 版本与自更新（四那节改的就是它） |

跑不了的（要真终端、要联网、要 GUI 交互的）**照实记下来**，别装成通过了。

---

## 八、发布 0.4.0

- `claude_tool/__init__.py:13` 的 `__version__` 0.3.1 → 0.4.0。这是界面重构 + 新
  公开接口，够一个 minor。
- `docs/releases.js` 加一条（含 windows/linux 产物名、大小、`claude` 字段）。
- `build/` 下打包，Inno 自动递归收。
- **推 gitee 前把 diff、版本号、releases 条目摆出来过一眼** —— 对外动作，这一版
  改动大，值得多看一眼。

**已发布**：2026-09-24 打的 0.4.0（Windows 安装包 `ClaudeLauncher-0.4.0-Setup.exe`，
9.81 MB；那一条记录里的 `claude` 格是打包时读本机 `claude --version` 填的
2.1.150）。另：frozen 冒烟探针改成**离屏**起 exe —— 见 `_probe_frozen_selfup.py`
的 `park_profile`，起之前先把沙箱配置里的窗口位置写到屏幕外，全程不上屏。

### 建议顺序

1. 跑探针存基线（七）
2. 拆 `ui/launcher.py`（三）—— 先拆不改行为，跑一遍探针确认没崩
3. 主窗重做 + 「当前工作区」（一）
4. 设置窗（二）+ 自更新去勾（四）
5. 插件宿主（五）
6. 探针全量回归（七）
7. novel_assistant（六）+ 作品集协议
8. 发布（八）

---

## 九、尺寸类探针的新期望值（0.4 实测）

主窗改成左导航 + 右详情之后，有几条探针量的对象没了（它们量的是搬走的那几块）。
按七那节的规矩，**重算期望值，不是把界面改回去迁就探针**。全量结果：**36 通过 /
2 环境性红 / 0 真回归**（红的两条是重构前就红的那两条，理由见
`temp/probes/BASELINE.md` 第 3 节）。

### 新期望值

| 项 | 0.4 之前 | 现在 |
|---|---|---|
| 左导航 `NAV_WIDTH` | — | 240（定宽） |
| 右栏「管理」那排，不含「删交接文档」 | — | 324 |
| 右栏「管理」那排，**含**「删交接文档」 | — | 441 |
| `DETAIL_MIN_WIDTH` | —（新引入） | 390 → **460** |
| 正文两侧页面空档 `PAGE_GUTTER` | 14 / 16 / 20 三套 | **20**（统一） |
| **窗口下限** | 713 × 660 | **804 × 660** |

下限那条式子是：

```python
content = NAV_WIDTH + PAGE_PAD + DETAIL_MIN_WIDTH      # 240 + 16 + 460 = 716
width   = content + 2 * PAGE_GUTTER + MIN_MARGIN       # + 40 + 48 = 804
```

**两侧那道缝必须跟 `_build_ui` 里 body 真正用的常数一致**。收尾前 body 用的是
`SIDE_GAP`(14) 而式子按 `PAGE_PAD`(16) 算，差的那 4 像素一直被 `MIN_MARGIN` 悄悄
吃掉——式子看着对，其实跟现实差一截。

`_probe_winsize` 和 `_probe_embed` 一个字没改（量的东西跟列宽换了无关）；
`_probe_footer_align`、`_probe_size_state`、`_probe_size_dump`、`_probe_width`
改成从新位置量。探针里**别把下限写成死数**，跟上面那条式子对（`_probe_autonomy`
原来写死 `<= 760` 上界，Task 8 就红了）。

### 重算时发现的两个真问题（已修）

1. **`DETAIL_MIN_WIDTH` 定小了**。Task 6 是**估**的（按「删交接文档 96 像素」），
   实测那颗按钮 111——最宽状态要 441，而当时最窄窗口只给右栏 442，余 1 像素。
   而 `detail.py` 里那个常数的注释写的就是"保证那排**六个**按钮还在同一行上"，
   它当时不满足自己写的意图。按实测改成 460。
2. **`_sync_handoff_button` 用错了谓词**：`winfo_ismapped()` 问的是"此刻可见吗"，
   离屏跑时一个 pack 着的按钮也报 0，于是按钮撤不下去（切到没交接文档的那本还
   挂着它）。改用 `winfo_manager() == "pack"`。

### 收尾又量出两个（已修）：页面空档不一致、右栏几块错位

用户报"主页面有些功能区域不对齐"。新写 `_probe_align.py` 把各块的位置量成一张表，
两个问题都露出来了——**这两条探针之前都测不到**：`_probe_navdet` 量的是"按钮在
不在、那排换没换行"，`_probe_size_state` 量的是"那排**自然宽**够不够"，没有一条
问过"这块**实际**摆在哪"。

1. **页面空档三套数**。顶栏那条线和状态行文字是 20、正文两栏是 14（图省事复用了
   `SIDE_GAP`）、告警区又是 16。并排一放就看得出：左导航那栏比顶栏和状态行的字
   凸出去 6 像素，右栏内容又比顶栏那颗按钮凸出去 6 像素。

   引一个 `theme.PAGE_GUTTER = 20`（页面四条边的统一空档），顶栏、正文、状态行、
   告警条五处都指它。跟 `SIDE_GAP` 的分工写进 theme.py 那段注释了：那个是"两栏
   之间"的缝，这个是"内容离窗口边"的缝。

   `PAGE_PAD`(16) 留着，但现在只管"左右两栏之间那道缝"这一件事。

2. **右栏几块整体错位**（这条是硬 bug，不是观感问题）。三颗日常动作按钮（新会话/
   接着上次/打开目录）是直接 `pack(side="left")` 进**右栏本身**的，而右栏别的孩子
   全是默认 `side="top"` 一路往下摞。pack 的规矩是：`side="left"` 的孩子从**当前
   剩余区域**的左边缘切一条走，而且没写 `fill="y"` 就纵向居中。于是——

   | 量到什么 | 收尾前 | 修完 |
   |---|---|---|
   | 日常动作那排的 y | 553（飘在整栏中间，整栏 857 高） | 244（贴在详情标题下面） |
   | 「管理」那排的左缘 / 宽 | 547 / **339** | 276 / **604**（撑满右栏） |
   | 插件区的左缘 / 宽 | 547 / **339** | 276 / **604** |
   | 「管理」那排最宽状态要 441，只给 339 | **「删交接文档」被裁掉** | 摆得下，余量 ≥ 20 |

   最后一行是这条 bug 真正的代价：有交接文档时，那颗「删交接文档」按钮**根本看
   不见**（超出框外被裁），而 `_probe_navdet` 还在绿着——它只问 `winfo_manager()`
   是不是 "pack"，只问那排**自然宽**够不够，从没问过"框实际分到多宽"。

   修法：日常动作自己占一个行框（`detail_daily_row`），三颗按钮 `side="left"` 摆进
   它里面；「管理」那排的框留个引用（`detail_manage_row`）给探针量实际宽度。顺手把
   `_probe_navdet` 那条余量断言从"拿 `detail_area` 宽度比"改成"拿那排自己框的宽度
   比"——按 `detail_area` 比是看不出这个 bug 的。

   `_probe_align.py` 现在把这两件事都钉住了（默认尺寸 + 缩到下限各量一遍）：
   页面五处边缘同一条线、右栏三块的左缘跟宽度、右栏四块的纵向顺序、那排摆得下最
   宽状态且余量 ≥ 20。

### 探针沙箱：一条不能踩的线（这轮被咬了一口）

`paths.py` 是在**它自己 import 那一下**把 `~` expanduser 成模块级常量的：

```python
TOOL_DIR = os.path.join(os.path.expanduser("~"), ".claude_tool")
```

所以探针必须先设 `USERPROFILE`、**再** import `claude_tool`。反过来写的话常量冻在
真实家目录上，探针读的是沙箱、写的是**用户的真配置**——而且**不报错**，断言照绿。

这轮两个探针踩了：`_probe_width.py` 压根没设沙箱；`_probe_embed_glue.py` 把
import 写在沙箱前面（第 30 行 import、第 116 行才设）。后者跑一趟把用户存的窗口
位置改了。

加了个静态检查堵这一类：

```bash
python temp/probes/task8/sandbox_order.py   # 套件跑之前先过这一关
```

它对每个探针取"第一次设 USERPROFILE"和"第一次 import claude_tool"的行号，判红
"设晚了"和"整份没设却要建窗口"。AST 判的，不是正则猜。

