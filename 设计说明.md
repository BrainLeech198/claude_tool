# 设计说明

这个启动器是干什么的、代码怎么切、为什么这么切。改代码之前先看这份。

## 它解决什么问题

Claude Code 原生的用法是 `cd 到项目目录，敲 claude`。两个麻烦：

1. **换模型要手改 `~/.claude/settings.json`。** 国内几个模型的 endpoint 和
   模型名各不相同，每次改都得翻文档、抄地址、贴 key。
2. **工作区散在硬盘各处。** 每个项目都要 `cd` 过去，记不住路径。

所以它做了两件事：把模型预设存成一个个 json，点一下就覆盖
`~/.claude/settings.json`；把工作区存成一个列表，点一下就在那个目录开窗口
跑 claude。两块互不干扰，启动前随时换模型。

## 数据放在哪

**启动器自己的东西全在 `~/.claude_tool/` 下面**，跟 Claude Code 的配置分开：

| 路径 | 装什么 |
| --- | --- |
| `launcher.json` | 工作区列表、窗口位置、默认工作区目录、底下那排勾、「AI 托管」上次选的档和工作区 |
| `claude_settings/*.json` | 模型预设，一个模型一个 json |
| `workplace/` | 默认工作区目录，「新建一个文件夹」就在这儿建 |
| `hooks/hook.json` | 挂 Stop hook 用的那份配置，启动时用 `--settings` 临时交给 Claude Code |
| `handoff_state.json` | hook 那两个行为的节流计数，按目录分开记（刷文档的轮数/时刻、自动继续的连计数） |

**唯一的例外是 `~/.claude/settings.json`**——那是 Claude Code 自己的文件，
换模型是往里覆盖一份新内容。除此之外不碰 Claude Code 的任何东西。

这一点直接决定了安装包的行为：**卸载时不能删 `~/.claude_tool/`**，那里面
是用户全部的预设（含 API key）和工作区列表。卸载程序只留一句提示。

## 代码怎么切

```
claude_tool/               包
├─ __main__.py             入口：--hook 走 hook 那条路，否则开窗口
├─ paths.py                所有目录/文件/命名规则常量
├─ theme.py                配色、字体、量文字宽度
├─ permissions.py          权限等级表
├─ config.py               launcher.json 读写、工作区扫描、上次聊时间
├─ presets.py              模型预设 + 常用供应商表
├─ claude.py               找 claude、拼命令行、启动
├─ handoff.py              交接文档的提示词 + Stop hook 本体（挂在这儿的两个行为都在这儿）
├─ versions.py             本机 claude 要不要更新：解版本号 + npm / 官网两个源
├─ host.py                 系统层门面：按平台转发给下面那两个
├─ winhost.py              Windows：内嵌 conhost、认领/挪位/置顶/关闭窗口、丢回收站
├─ nixhost.py              Linux / macOS：pid 当句柄、挑终端起窗口、丢回收站
├─ mover.py                搬工作区：复制 → 对账 → 旧的丢回收站
├─ widgets.py              自绘控件：胶囊按钮、列表行、滚动区、表单工具
└─ ui/
    ├─ launcher.py         主窗口（最大的一个文件，布局和交互都在里面）
    └─ dialogs.py          那几个对话框
```

行数不写在这份文档里——它每加一个功能就变，写死只会变成一条迟早不对的信息。

### 对话框为什么是 mixin

`dialogs.py` 里的 `LauncherDialogs` 是个 mixin，`class Launcher(LauncherDialogs,
tk.Tk)` 继承它。**方法体一行没改**——它们本来就全是 `self.xxx`，跟主窗口共用
`config_data`、`feedback_var`、`refresh_*` 这些状态。

没做成"把状态收进一个独立的对象再注入"，是因为那要挨个改方法体（每个
`self.config_data` 都得改成 `panel.config_data`），跟"只是把文件切开"是两码事，
风险也不在一个量级。真要那么做，应该连同 `launcher.py` 一起，按面板
（模型面板 / 工作区面板 / 会话面板）重新划分职责，而不是先动对话框。

留在 `launcher.py` 的是**面板自己会调**的那些：`remove_model`、`switch_model`、
`launch_workspace`、`start_autonomy`、`rescan`，还有 `_set_workplace` /
`pick_workplace` / `refresh_workplace_label`——「默认工作区」是主界面那行和
「新建一个文件夹」那个对话框共用的一个设置，改的地方两处走同一条路，落盘和
刷新界面也就只有一处。

依赖是单向的，从下往上：

```
paths / permissions / theme / versions   谁都不依赖
        ↓
      claude  ← handoff / presets
        ↓
  winhost / nixhost                （依赖 claude）
        ↓
       host        按平台转发给上面两个；上层只 import 这里
        ↓
      config        （依赖 paths + permissions）
        ↓
      mover        复制 → 对账 → 丢回收站（依赖 config + host）
        ↓
     widgets        （依赖 theme）
        ↓
        ui          （依赖所有）
```

这摞东西是按"层级"排的，不是严格的逐个依赖：`config` 并不 import `host`，
`widgets` 也只依赖 `theme`——每一行右边注的才是它真正依赖谁。

**`winhost` 和 `nixhost` 导出同一组名字，缺一个 `host.py` 就当场 AttributeError。**
要的正是这种当面炸掉，而不是到某个平台上跑到一半才发现少了个函数。所以非 Windows
上那些真做不到的（内嵌终端）也在 `nixhost.py` 里留着同名占位，调用就抛
`NotImplementedError`——名字表齐不齐是结构问题，做不做得到是产品决定，别混在一起。

**只有 `ui/` 会 import tkinter。** 这不是洁癖，是硬要求：`--hook`
那条路上不能碰 tkinter，也不能弹任何窗口——那是 Claude Code 在跑对话的
中途回头调的。所以 `__main__.py` 把 hook 分支单独拎出来，连主界面都不 import
（在 `main()` 里面才 import），保证这条路干干净净。

同理，`claude_tool/__init__.py` 是空的。它一旦往里放 import，上面那条
隔离就白做了。

## 会话跑起来之后

对已经跑起来的 claude，启动器**没有输入通道**（Windows 上没法往一个开着的交互式
会话里塞按键），能做的只有窗口层面的操作（挪位、置顶、关掉）和观察。所以下面每一步
都是"拍快照、做差、看文件时间戳"，没有一步是"让它干点什么"。

**新窗口那条路。** 启动前先 `terminal_windows()` 拍一张现有窗口的快照，启动后再拍一张，
差集里就是这次新开的那扇（都在 `winhost.py`）。所以要**先拍后启**——等它冒出来再去数，
就分不清哪扇是这次新开的、哪扇是上一轮留下的。默认终端是 Windows Terminal，新窗口的
类名是 `CASCADIA_HOSTING_WINDOW_CLASS`；按 `ConsoleWindowClass` 找一个都找不到，
两个类名都得认。

**做差有个根本限制。** Windows Terminal 要是被设成"新窗口开成已有窗口的标签页"，
桌面上的窗口数压根不变，做差永远认不出来——那种配置下「移过来 / 置顶 / 关掉」那三个
按钮不会出现。这是限制，不是没修完。

### 内嵌那条路：贴上去，不是塞进去

内嵌会拉一扇老 conhost 窗口（为什么不用 Windows Terminal，见 `winhost.py` 开头），
把它**贴**在终端栏那块矩形上：认启动器当 owner、去掉标题栏、带上 `WS_EX_TOOLWINDOW`
（任务栏和 Alt+Tab 里都不露面），位置跟着终端栏走。owner 是白拿的——跟着启动器一起
最小化、启动器一销毁它跟着销毁。

**不能用 `SetParent` 塞成子窗口。** 早先那版就是塞的，症状是那个窗口点不进去、打字
全是没反应。根子在 Windows 的焦点是**按线程的输入队列**分的，不按窗口层级走：子窗口
归 conhost 那条队列，我们的线程进不去，给它 `SetFocus` 一律 `ERROR_ACCESS_DENIED`；
鼠标倒是通的（点到哪儿系统认的就是那扇窗口）。正规解法 `AttachThreadInput` 对 conhost
挂不上，回 `ERROR_INVALID_PARAMETER`——同一台机器上挂本进程别的线程、挂别的 GUI 进程
都是一挂就成，conhost 的线程 id 也有效（`OpenThread` 拿得到句柄）、正着反着都挂不上，
所以不是参数写错，是系统不让。做成 owner 窗口就绕开了这件事：它吃自己那条队列的焦点，
键鼠全是原生的。

代价是它终究还是一扇独立窗口：不会被启动器窗口裁掉，别的窗口盖到启动器上时它也跟着
被盖。另外**窗口自身的挪动它跟不动**——Tk 只在顶层窗口挪动时给顶层发 `<Configure>`，
终端栏一个都不发（量过），所以顶层上另绑了一个 `_on_window_move` 负责重新贴。

**换模型的时候接着聊。** `switch_model()` 覆盖完 `~/.claude/settings.json` 之后，
`_offer_migration()` 逐个会话弹框问要不要接着聊；答应的话走一条流水线：让旧会话先写
一份交接文档 → 关掉旧窗口 → 用新模型 `--continue` 重开，并带上"先读 handoff.md"的
开场白。**这份交接文档是新模型写的，不是旧模型**——那时候 `settings.json` 已经被覆盖
了，`--continue` 起来的会话读到的就是新预设。主界面顶上那块进度区（走马灯 + 第几个 +
当前步骤 + 已用秒数）就是这条流水线的门面，手动点「整理交接文档」也走同一块。

**两步之间的等待是等条件，不是等毫秒。** 关掉旧会话之后要等它真退干净再开新的（否
则两扇窗口撞在同一块地方）；新会话开出来之后要等"这扇窗口认出来了"再开下一个（否则
两次开窗口的新窗口会挤进同一次快照做差里，句柄认串）。两处都走 `_wait_until()`：每
200ms 看一次条件，成立就走；最多等约 15 秒，到点也不卡死——把"没等利索"摆到进度条
上照常往下走，后面各步自己还有兜底。

"认出来了"这个戳是 `_watch_terminal` 盖的（`watched`）：认到句柄、会话退了、或者
30 秒超时都算收工。内嵌那条路不用等——那扇窗口是贴在本窗口上的一栏、不进窗口清单
做差，而 `_run_next_task()` 自己会躲着 `_pending`。

**同一份 handoff.md 只许一个写手。** 会话自己的 Stop hook 也可能正在刷这份文档
（见「自主接手」）。撞上时：手动的「整理交接文档」被挡回来并说清后果；换模型的流水线
把那个会话让到队尾，等锁撤了再补一份——hook 那份可能漏掉它停下之前的最后一点新对话。
启动器自己写完之后会顶掉 hook 的节流时间戳，免得两边来回往同一份文件上写。

## 默认工作区摆在明面上

「工作区」标题底下第二行就是它：`默认工作区  <路径>  [更改目录] [打开]`。它管的是
那张列表里的东西**从哪儿冒出来**——「新建一个文件夹」建在哪儿、扫描扫的是哪儿——
所以摆在离列表最近的地方，跟「AI 托管」那行一个理由。

这行早先撤过一次，撤的理由是"主窗口上摆不下"。撤出来的后果是：改这个设置只剩
「新建一个文件夹」对话框里一条路，而那条路**点了「更改目录」还要点「创建」才落盘**
——选完点「取消」就白选一场，界面上还没有任何提示，看起来就成了"我明明改过，怎么
新建的文件夹还是老地方"。所以这行请回来了，同时对话框里那个按钮改成用同一个
`pick_workplace`：**选中当场落盘**，点「取消」取消的只是"建这个文件夹"这件事。

路径是只读的一行字，不是输入框——手敲路径容易打错，选目录交给系统那个选择框。
换完不会自动重扫，反馈那行会提一句「点『重新扫描』能拉进列表」：自动扫会把新目录下
一堆子文件夹一口气塞进列表，那是另一件事，不该跟着这一次点击发生。

## 搬工作区

工作区那行最左边有个「搬迁」：整个文件夹搬到新位置（末尾那段可以直接改掉当改名用）。
搬的是**两样东西**——那个文件夹本身，和 `~/.claude/projects` 下面存它会话记录的那个
目录。后者不跟着搬的话，新位置在启动器眼里就是个没聊过的新工作区，「接着上次聊」
接不上。

**顺序是死的：检查 → 复制 → 对账 → 改配置 → 丢回收站。** `mover.py` 里那句
"先复制、后删旧的，中间还要对一次账"是这个功能全部的意义所在：任何一步失败就停在
那儿——新复制的那份留着、旧的一个不动，最坏是"多了一份拷贝，你自己删一下"，不会是
"东西没了"。所以**没有回滚**：走到丢回收站那一步时新的那份已经对过账、配置也指过去
了，这时候把旧的搬回来才是真危险（配置指着新的，旧的又冒出来，两边都在）。

配置那一步夹在中间、而且必须成功：写不成就不丢旧的——新的那份留在那儿（大不了用户
自己删），旧的原封不动、配置也还指着旧的，至少系统是自洽的。反过来先丢旧的再存配置，
中间一失败就是两边都没了。

**对账只认路径和大小，不认 mtime。** 跨文件系统复制时 mtime 的精度对不上（FAT 两秒
一颗、ext4 纳秒），拿它当判据会误报一片。对账主要兜的是另外三件事：复制过程中源还在
被写、盘满了写了一半、权限之类的边角没带过去。

**复制是自己走树，不用 `shutil.copytree`。** copytree 是黑盒，中间一点消息都报不出来，
搬一个几个 G 的目录就是干等着、界面看着像死了。自己走就能一路把"到哪儿了"报出去——
进度跑在后台线程里，走队列回主线程（Tk 控件只能主线程碰）。目录的时间戳留到最后再补，
先补的话只读的目录后面就写不进去了。

**开工前有两件事只有界面知道，比 `mover` 里任何一条检查都要紧**：这个工作区的会话
正开着、或者它的 Stop hook 正在刷交接文档。那两种情况下文件夹正被写，复制出来的是
半截的东西，对账也可能通不过——而这两件事从文件系统上看不出来。所以拦在
`move_workspace_to()` 的开头，`mover.check()` 只管文件系统和配置那一层。

**丢回收站是真回收站，不是永久删。** Windows 走 `SHFileOperationW` + `FOF_ALLOWUNDO`
（shell32 的普通导出，不是 COM）；Linux 找 `gio trash` / `trash-put`；macOS 挪进
`~/.Trash`。工具一个都找不着就**不删**、如实报出来让用户自己处理，绝不拿 `rm -rf`
兜底。Windows 那边还有个坑：盘上没有回收站（网络盘、可移动盘）时它会退化成永久删
并且**照样返回 0**，从返回值上看不出来。

搬工作区和换模型那条流水线**共用同一块进度区**，所以两者互斥（`_busy()`），手动点
「整理交接文档」正在写的那会儿也得挡（那条路不占 `_task_running`，但它也摆着同一块
进度区、写完还会把进度区收回去）。都不挡的话，进度条和那几行字会被两边抢着改。

这块动的是真文件，所以分两层各验一遍，都在沙箱里（`USERPROFILE` 指到临时目录，
绝不碰真实的 `~/.claude`）：

- `_probe_mover.py` 撞 `mover.py` 这一层：真建两棵树、真搬、真进回收站，逐条比
  新旧两棵树；外加每一处"该拒就拒"（目标已存在、目标在源里面、源不存在、上层目录
  不存在、那边已有会话记录），并确认**拒的时候源一个字节没动**。
- `_probe_move.py` 撞界面这一层：进度区摆出来没有、复制完配置改没改、旧的真进没进
  回收站、名字跟不跟着新文件夹名走、以及两条只有在沙箱里才撞得到的失败路径——
  复制中途报错（旧的必须没动）和配置写不成（不许先把旧的丢了）。

## 两个必须知道的坑

### hook 命令行指向哪儿

`handoff.py` 里 `hook_command()` 拼的这条命令行给到 Claude Code，让它每次停下来
回头调我们。两种形态，靠 `getattr(sys, "frozen", False)` 分叉：

```
源码：  "python.exe" "…\claude_tool\claude_tool\__main__.py" --hook --handoff --auto-continue
打包后："C:\…\Programs\claude_tool\claude_tool.exe" --hook --handoff --auto-continue
```

`--hook` 是入口标记，后面跟的是**各行为自己的开关**：`--handoff`（自己刷交接文档）
和 `--auto-continue`（停下时接着往下推，见「自主接手」）。这样加第三个行为时不用
再开一条入口路径，`__main__.py` 里也只有一处分支。勾了哪些就写进那一份
`hooks/hook.json`，勾选状态就此固化——改勾不影响已经跑着的会话。
（老版本的入口标记是 `--handoff-hook`，配置在 `hooks/auto_handoff.json`，
启动时顺手删掉那份旧配置。）

源码那条**不能写 `handoff.py` 自己**——那样会被当成脚本直接跑，包内的
`from claude_tool.xxx import` 全部失败。同理 `__main__.py` 里有一段 `sys.path`
引导（冻结时跳过），就是为了让它既能被 `python __main__.py` 直接跑，也能被
`python -m claude_tool` 跑。

路径一律带引号：装到 Program Files 的话里面是有空格的。

### 标准输入输出的编码

**进的那一头。** 这台机器 locale 是 cp936。Python 走管道时 `sys.stdin` 默认按
GBK 解码，而 Claude Code 给的是 UTF-8。只要路径里有中文（工作区叫「冉」
「小说」这种），`json.load(sys.stdin)` 就抛 `UnicodeDecodeError`，被 except
吞掉，表现为"这个功能对中文工作区永远不触发"，且没有任何报错。

所以读的是字节流、自己解 UTF-8（`_stdin_bytes()` + `decode("utf-8", "replace")`）。
**任何新加的、跟外部进程走标准输入输出传数据的地方，都要显式按字节 + UTF-8 处理。**

**出的那一头。** 冻结后这个进程没有控制台，`sys.stdout` 是按系统 locale 开的，
往里写中文就有风险。所以 `json.dumps` 的 `ensure_ascii` 必须保持 True——转义成
纯 ASCII 的 `\uXXXX` 才不挑编码。这不是"默认值无所谓"的事，改成 False 会变成
一个只在中文提示词上才炸的 bug。

**顺带一个反直觉的实测结论。** 这份文档原先写着"`--noconsole` 的 exe 里
`sys.stdin` / `sys.stdout` 是 `None`"。真冻出来撞了一下（PyInstaller
`--noconsole`，管道接在 fd 0/1 上），三个流**都有值**——有句柄 Python 就给赋值，
跟有没有控制台不是一回事。所以 `_ensure_stdio()` 那个兜底不是主路径，留着是防
句柄没传进来的情形：那时 `sys.stdout` 才是 None，而 **print 到 None 是静默无操作**，
block 答复会凭空消失且不报错。这种漏法不值得省那十行。

## 自主接手：别让会话停下来等人

用户发一个目标就离开电脑，是这套东西想做的下一个能力。**麻烦在于开了
`bypassPermissions` 也停不住**：那个档位免掉的只是工具确认，"用 A 方案还是 B 方案"
这类选择 Claude 照样会摆到人面前（`AskUserQuestion` 不受权限等级管，直接用文字
提问同理），所以人还是得坐在那儿。

**能插手的只有一个地方：Stop hook。** Windows 上没法从外部往一个开着的交互式会话
里投喂输入——会话跑起来之后启动器只能观察它。唯一能反过来影响它的，就是 Claude Code
每轮把控制权交还给用户之前跑的那一次 Stop hook：回一句
`{"decision":"block","reason":"…"}` 就能逼它再干一轮。自动刷交接文档和自动继续架的
是同一个杠杆，所以共用一个入口。

### 入口：主窗口那个「AI 托管」按钮

按"接手到什么程度"分档，往上加档，用户自己选。

入口是「工作区」标题底下那两行里第一行的 **「AI 托管」**按钮——摆在那儿而不是顶栏，
是因为托管要挑的就是一个工作区，跟"在哪儿挑工作区"本来就是同一件事。按下去弹一个
对话框：挑工作区（默认上次托管的那个）、写一段任务目标、选一档。点「开始托管」= 在那个
工作区起一个**新**会话（不带 `--continue`），任务目标当命令行上的位置参数送进去当
第一条用户消息，然后按那档挂 hook。两件事都撞过：`claude "任务"` 不带 `--continue`
时开场白确实成了会话记录里的第一条用户消息（2026-09-19 用一个临时探针实测）。

档位和上次用的工作区记在 `launcher.json` 的 `autonomy` / `autonomy_workspace` 里，
下次打开对话框直接停在原处；写坏的档位（4、字符串）一律回落第 1 档。

对话框里**摆着这个工作区的权限等级并附一句警告**，因为"它问你话"和"它等你点允许"
是两种不同的停顿：**Stop hook 只接得住前一种**。权限不够高时用户走开一趟，回来会
发现它卡在权限确认上——那不是托管失灵，是权限的事。这句话是为了别让人误会。

底下那排的**「自动继续」勾还在**，管的是"平时随手开一个会话要不要自动继续"，
跟托管对话框里当场选的那一档互不影响。托管那次会话把两者**取并集**：底下勾着
「自动刷交接文档」的话，托管会话两个 hook 都挂；只勾托管，就只挂自动继续。

| 档 | 干什么 | 状态 |
| --- | --- | --- |
| 1. 让它自己定 | 它停下来等你的话，一律回一句「接着干，自己定」 | 已落地一半（`--auto-continue` 管的是 `Stop` 那种停；它用 `AskUserQuestion` 摆选项问你那半还没做） |
| 2. 接指挥模型 | 让一个小模型读交接文档 + 最后一条助手消息，答它真正问的那个问题 | 没做，对话框里灰着 |
| 3. 半指挥 | 平时让它自己跑，碰上改 git 历史、大范围重做这种，停下来等你 | 没做，对话框里灰着 |

**第 1 档原来还分过一个「替它选」**（`PreToolUse` 拦下 `AskUserQuestion`、把选项答案喂
回去）。后来合并掉了：那条路没人来选的时候，兜底还是回它一句"你自己定"，跟第 1 档
一个效果——两个档摆着一个样的东西，不如并成一个。合并之后第 1 档要管两种停：
`Stop`（已经做了）和 `AskUserQuestion`（还没做）。

第 1 档**故意不接第二个模型**，就回一句固定的话：先量出这条路到底能吃下多少停顿，
再决定值不值得上一个指挥模型。零成本，坏了也坏不到哪儿去。

第 2、3 档**列出来但灰着**。列出来是因为用户得看得见这条路往上还有什么；灰着是
因为点下去就得装作托管着——宁可让他知道还没做。

第 3 档跟第 1、2 档是**反方向**的：前两档是"别回来烦我"，第 3 档是"平时别回来烦我，
但这几件事必须先问我"。要划的那条线（现在想到的）：改 git 历史（`rebase` / `reset
--hard` / `push --force` / 改已提交的历史）、大范围重做（几十个文件一起动、推倒重来
的那种）、删东西。**判据还没定**——是让模型自己按提示词判断、还是 `PreToolUse` 拦
具体命令，两条路都还没撞过。

### 第 1 档的护栏

- **连着续 `AUTO_CONTINUE_MAX`（8）次就放行。** 平台自己对连着拦的次数也有限制，
  取 8 是照文档里那句"一次停顿大约连着拦 8 轮会被强制收尾"来的（这个数没实测过，
  反正比它小就安全）。
- **距上次续超过 `AUTO_CONTINUE_RESET`（10 分钟）就把连计数清零。** 中间隔了十分钟，
  要么是用户回来敲过字、要么是模型自己在干长活，两种情况都该重新开始帮它往前推。
  这也顺带兜住了总量——不在那儿清的话，一个卡住的会话会被无限期地催下去。
- **它自己说「已完成」就停。** 模型说这句就是收工了，别再去催一个做完的会话往下编。
- **`stop_hook_active` 这条故意不认。** 那个标记只有"被 hook 逼出来的那一轮"才带；
  自动继续要是认了它，一次对话最多只能续一轮，等于没这个功能。护栏改由上面那个
  连计数自己数。（刷交接文档那条反过来，**必须认**——认了才不会来回往同一份文件上写。）

### 后面几档要接的东西（先记着，真做的时候逐条复核）

**第 1 档剩下那半（`AskUserQuestion`）：**

- **它和 `Stop` 不是一回事**：`AskUserQuestion` 不走权限确认（`bypassPermissions` 也
  拦不住它）、`Stop` 不为它触发。所以那半得挂在 `PreToolUse` 上，由 `__main__.py` 那个
  唯一的 hook 入口按 `hook_event_name` 分发（加行为别另开入口，这条已经在遵守了）。
- **回什么让它"自己定"还没撞过**。回 `allow` + `updatedInput`（带 `answers`）是"替它选
  好答案"，等于要把答案编出来——那正是被合并掉的那条老路。想让第 1 档保持"不编答案"，
  大概得回 `deny` + 一句「用户不在，按你自己的判断定」，让被挡掉的工具调用连理由一起
  当成指令读。**这只是猜的**（现在文档里"回 deny 会让它重新问一遍"那句也是猜的），
  真做的时候先拿一个探针把 `deny` / `allow+updatedInput` 各自的行为撞清楚。
- **别弄成死循环**：万一 `deny` 之后它又摆一次选项，就得跟 `--auto-continue` 一样有个
  连计数兜底，连着挡几次就放行。

**第 2 档（接指挥模型）：**

- **指挥模型从哪来**：这台机器的 `~/.claude/settings.json` 里 `ANTHROPIC_BASE_URL`
  指着别的供应商，`--model haiku` 这类内置名字会一起被路由过去——所以指挥模型得从
  用户自己的预设文件里挑。调用形如 `claude -p --settings <那份预设>`。
- **它只答，不做**：「谁来答它的问题」和「谁允许它动手」是两个旋钮，各管各的。指挥
  模型答完之后，真正执行仍然按那个会话自己的权限等级走。
- **答不出来就放行**：指挥模型超时、报错、答非所问，一律 fail open——照常停下来问
  用户。不能因为一个答不上来的副手把用户的目标卡死在半路。

**第 3 档（半指挥）：**

- **判据放哪儿**有两套走法，都还没撞过：写进 hook 回的那句 reason 里（让模型自己按提示
  词判断"这算不算大改动"，省事但靠自觉），或者挂 `PreToolUse` 拦具体命令（认得出
  `git reset --hard`，但认不出"这次重做主界面"这种语义上的大动作）。真做的时候大概
  得两条一起上。
- **"呼叫用户"本身没有现成办法**：Windows 上没法往一个开着的会话里投喂输入（见上面
  「会话跑起来之后」），能做的只有"停下别推它"——也就是这一档**不挂自动继续**，让它
  照常停在提示符上等。想要真有声响，得另外想办法（托盘气泡、响一声之类），那是另一件
  事，现在没打算做。

## 查 claude 新版

启动器配的是**外部那个 claude**，它自己在更新，两边版本会越差越远。所以做了两件事：
官网上写清"这一版配着哪个 claude 测过"，启动器里能查本机这版要不要更新。

**官网那一格是打包时自动读的**（见 `update_releases.py` 里的 `local_claude()`）：
跑一次打包机上的 `claude --version`，把号写进 `docs/releases.js` 那条记录的
`claude` 字段。读不到（没装 claude、或者输出里没有数字）就留空，页面上那一格
不画——跟 `notes` 是同一个套路。重打同一个版本时**只补空的、不改已经写过的**：
重打包不等于重新测过，别让"最近一次打包时本机恰好是哪个版本"悄悄改掉已经发出去
的那句话。

**启动器这边两个源**（`versions.py`）：

- **npm registry** 上是 claude 真正的最新版，问得到就用它。
- **官网的 releases.js** 是兜底：npm 问不到（没网、被墙）时，拿"打包时记下的那个
  版本"比一比。它比 npm 旧，所以只当备用，不当准。

**联网这件事默认是关的**——底下那排勾里的「自动查 claude 新版（联网）」不勾，
启动时一次网都不联（`auto_version_check`，跟另外几个勾一样首次装上一定是关的）。
勾上之后启动时问一次；**顶栏那个「查更新」任何时候都能手动查一次，不受勾管**。

查出本机落后，就在版本号右边冒一个「有新版 X.X.X」的胶囊，点开是官方的安装/升级
说明页（跟"没找到 claude"那条告警上的「打开官网说明」是同一个地址），反馈栏里说一
句本机是多少、网上是多少。

三条边界，改之前先看：

- **只提醒，不替用户升级。** 不跑 npm、不动 claude 的任何文件，也不替用户装东西
  ——装的是个外部程序，升不升、什么时候升是用户自己的事。
- **查不到就闭嘴。** 联网失败、解析不出来，一律返回 None，不抛异常、不弹框：自动
  那次一声不吭，手动那次在反馈栏里说一声（用户点了按钮，得知道发生了啥）。
  npm 和官网都是"外面的东西"，哪天接口变了不该把启动器带崩。
- **版本号只前几段比大小**，短的补零（`2.1` = `2.1.0`）。本机版本号读不出来时不判
  "落后"——宁可不说，也别拿一个没读到的东西去吓用户。

官网那一页的渲染有个 node 写的探针：`node _probe_site.js`（假 DOM，几十行）。
它验的是"字段读对了、没写就不画"，不验真实排版。

## 怎么跑

```bat
运行.bat
```

等价于 `python claude_tool\__main__.py`。这条路上看得见报错，开发时用。
打包出来之后，给用户的是开始菜单里的快捷方式，不依赖机器上有没有 Python；
`dist\claude_tool\` 整个目录拷走也能直接双击跑，那份是免安装的。

单独试 hook（`cwd` 指到哪儿，节流就算在哪份记录上）：

```bat
echo {"cwd":"D:\\某目录"} | python claude_tool\__main__.py --hook --handoff
echo {"cwd":"D:\\某目录"} | python claude_tool\__main__.py --hook --auto-continue
```

什么都不打印 = 放行；`{"decision":"block","reason":…}` = 把 claude 拦下来再干一轮。
`_probe_autocontinue.py` 就是这么撞的——纯子进程，不需要开窗口，跑起来最快。

## 怎么打包

```bat
build\打包.bat          rem Windows
```
```sh
sh build/打包.sh        # Linux
```

Windows 一条龙：PyInstaller 出 `dist\claude_tool\`，Inno 再打成
`Output\ClaudeLauncher-<版本>-Setup.exe`，最后把这一版记进 `docs\releases.js`。
构建期要有 PyInstaller 和 Pillow（后者是 PyInstaller 往 exe 里嵌图标用的），跟打包
出来的程序无关——那个仍旧零第三方依赖。

Linux 少一步：PyInstaller 出 `dist/claude_tool/`，直接压成
`Output/ClaudeLauncher-<版本>-linux-<架构>.tar.gz`，没有安装包这一步——解开放着就能
跑。架构那段是 `uname -m` 现取的，不写死。构建期只要 PyInstaller 一样，装法就是
仓库里那个 venv：

```sh
python3 -m venv build/_venv && build/_venv/bin/pip install pyinstaller
```

打包脚本优先用它，没有就退回 PATH 上的 `python3`；两边都没有 PyInstaller 就当场
报错退出，不往下走。`build/_venv/` 在 .gitignore 里。

```
build/
├─ claude_tool.spec     PyInstaller：onedir + 无控制台，入口是 claude_tool/__main__.py
│                       两个平台共用一个，只按平台分叉图标那一项
├─ claude_tool.iss      Inno：安装包，版本号在这里改，是唯一一处（Linux 也读它）
├─ 打包.bat / 打包.sh   上面那几步 + 记一版，依次跑一遍
├─ update_releases.py   把刚打出来的这一版 upsert 进 docs/releases.js
│                       不加参数 = Windows（.bat 调的老命令），加 linux 记 Linux
├─ icon.png             图标源图
├─ make_icon.py         PNG → 多尺寸 ico，换图时才跑
└─ icon.ico             生成物，提交进仓库（平时打包不必重跑 make_icon）
```

`update_releases.py` 那两个平台**各写各的那一格**：先打 Windows 再打 Linux，第二次
是在已有那条上补 `linux`，`windows` 那格和手写的 `notes` 一个字节都不动。同一个
版本号重打只刷新本平台的 `file`/`size` 和日期。

几个额外的坑，都在 Linux 这一侧：

- **版本号仍然只有一处**（`claude_tool.iss` 的 `AppVersion`）。打包脚本用 `sed` 从
  那儿读出来拼 tar 的名字，`update_releases.py` 自己也再读一遍——同一处来源，各读
  各的，不在 shell 里另立一份。
- **`.sh` 必须是 LF 换行**。在 Windows 上编辑这个文件很容易被改成 CRLF，那样
  `/bin/sh` 会拿着 `\r` 去当命令名，报一堆莫名其妙的错。仓库里这个 .sh 是 LF 的，
  改完拿 `file` 或数一下 `\r\n` 确认。
- **Linux 那格认产物用通配**（`...-linux-*.tar.gz`）。因为架构是现取的，脚本猜不出
  全名；万一同一版打了两个架构，`Output/` 里会同时躺着两个，`releases.js` 只有一格
  放不下——这时候脚本不猜，直接报出来让你自己删一个。
- **`.sh` 没有 `.bat` 那个限制**。只有 cmd.exe 解析的 `.bat` 才必须纯 ASCII + CRLF，
  `.sh` 里的中文随便写。

几个决定，改之前先看：

- **onedir 不 onefile。** 启动快（不用每次解包到临时目录），杀软误报也少。
  反正 Inno 装的就是一整个目录，分发形态没差别。
- **exe 名和安装目录用 ASCII**（`claude_tool.exe`、`{autopf}\claude_tool`），
  中文只出现在显示名、快捷方式和开始菜单上。hook 命令行里要塞安装路径，
  全程 ASCII 能少一整类编码麻烦。
- **装的时候让用户自己选**（`PrivilegesRequiredOverridesAllowed=commandline dialog`）：
  默认免管理员装到 `%LOCALAPPDATA%\Programs`，想整机共用可以走 `/ALLUSERS`
  或在界面上切，`{autopf}` 会跟着变。
- **`AppId` 那个 GUID 定下来就别改**，升级安装靠它认出是同一个软件；改了会变成
  新旧并存两份，而不是覆盖升级。
- **`[Code]` 段里 `;` 不是注释符**，Pascal 只认 `//`。写成 `;` 会报
  `'BEGIN' expected`，而且报错行号指在注释上，看着莫名其妙。
- **`.iss` 必须带 UTF-8 BOM**，否则 Inno 按系统 ANSI（这台机器即 cp936）读，
  里面的中文全乱。
- 卸载**不碰** `~/.claude_tool/`：Inno 默认就不会去删没装过的路径，我们只额外留
  一句提示告诉用户数据还在。那句提示包了 `UninstallSilent` 判断——脚本自己弹的
  MsgBox 不受 `/SILENT` / `/SUPPRESSMSGBOXES` 管，不判断的话静默卸载会卡在一个
  没人看的框上。

### 官网和发版

站点在 `docs/`，就是给人下载用的那一页。零依赖零构建，从本地双击
`index.html` 也能看：

```
docs/
├─ index.html    整个页面，样式和渲染逻辑都从外面引
├─ style.css     配色是照 claude_tool/theme.py 抄的，官网和启动器得一个长相
├─ releases.js   版本清单：window.DOWNLOAD_BASE + window.RELEASES 数组
├─ crab.png      从仓库根拷的一份（Pages 只看得见 docs/ 里面的东西）
└─ .nojekyll     空文件，让 Pages 原样发这几个文件、别去跑 Jekyll
```

**版本清单是 `.js` 不是 `.json`**，因为 json 得走 `fetch`：本地双击打开会被 CORS
拦掉、页面直接空一片，托管起来也白白多一个"加载中"。写成 js 是同步加载的，页面
打开就有内容。

**发一版要做的**：改 `build\claude_tool.iss` 里的 `AppVersion` → 跑 `build\打包.bat`
（最后一步自动把新版补进 `releases.js`）→ 在 Gitee 和 GitHub 上**各**建一个 tag 叫
`<版本号>` 的发行版，把 `Output\` 里那个 Setup.exe 传成附件 → 提交推送。**传附件这步
是手动**的，两边都没有能直接调的 CLI。少传了附件，官网上点下载就是 404。

**Linux 那份多两步**：在 WSL 里跑 `build/打包.sh`（同样会自动补 `releases.js`），出的
`ClaudeLauncher-<版本>-linux-<arch>.tar.gz` 照样两边各传一份。一个发行版能挂多个附件，
Windows 和 Linux 那两个包挂在同一个 `<版本号>` 下面，附件名不一样，互不冲突。
`打包.sh` 里去官网记账那一步只动 `linux` 那一格，Windows 和 macOS 那两格原样留着。

**GitHub 那份是单独传的**：`releases.js` 里除了 `DOWNLOAD_BASE`（Gitee，主源）还有
一个 `MIRROR_BASE`（GitHub）。填了它，Windows 那张卡片和每条历史记录都会多一个
「GitHub 下载」的入口，指到 GitHub 发行版里的同一个附件。**发行版附件不跟着仓库镜像
走**——仓库代码是 Gitee 镜像是推过去的，但 release 附件是两边各传各的。所以发新版
只传了 Gitee、忘了传 GitHub，那个入口点下去就是 404。某一版不想给镜像链接，就在那条
记录里写 `"mirror": null`。

**tag 是版本号本身，不带 `v` 前缀**（`0.1.0`，不是 `v0.1.0`）。下载直链是按
`DOWNLOAD_BASE + "/" + 版本号 + "/" + 附件名` 拼的，tag 打错前缀页面上点下去就是
404——这个坑 0.1.0 发版时真撞过一次。想临时指到别处就在那条记录里填 `url`。

**目录名 `docs` 没得选**：Pages 的「Deploy from a branch」源目录只能选仓库根
（`/`）或 `/docs`，中间没有别的选项。站点放这儿，发布源设成 `master` + `/docs`，
走的就是 GitHub 自己那套静态发布，仓库里不需要任何工作流文件。设计说明为了给站点
腾地方，从 `docs/设计说明.md` 挪到了仓库根。

**别走 Actions 那条路**：早先试过把站点放 `website/`、用
`.github/workflows/pages.yml` 传产物，结果 Gitee 的仓库镜像往 GitHub 推的时候被
GitHub 整条拒掉——`refusing to allow a Personal Access Token to create or update
workflow ... without 'workflow' scope`。只要仓库里存在 `.github/workflows/` 下的
文件，镜像用的那个 PAT 就必须带 `workflow` 权限，否则镜像一次都推不过去。放
`docs/` 就没有这个文件，也就没有这道门槛。

**仓库是两个**：Gitee 是主仓库，源码和安装包都在那儿（国内下载快）。GitHub 那份
是 Gitee 的仓库镜像自动推过去的，用来挂 Pages；**发行版附件不跟着镜像走**，是两边
各传各的（见上面 `MIRROR_BASE` 那段）。所以官网上那个主源指的是 Gitee 的发行版，
改 `releases.js` 里的 `DOWNLOAD_BASE` 就能换。

**三个平台字段**：`releases.js` 每条里都有 `windows` / `linux` / `macos`，没有包就写
`null`，页面上那一格自动画成灰的「还没做」。有包了就把 `null` 换成
`{"file": "...", "size": ...}`——**下载地址不用在这儿写**，Gitee 和 GitHub 两个地址
都是拿上面的 base 加版本号、加文件名现拼的。要改的顶多是平台卡上那句说明（现在
Linux 那句是"64 位，解开放着就能跑"），它在 `docs/index.html` 的 `PLATFORMS` 里，
跟这儿是两处而不是一处。

**包里可以填一个可选的 `url`**：填了就直接拿它当下载地址，不填才按 `DOWNLOAD_BASE`
拼。所以换镜像源、或者某一版临时指到别处，都不用动页面。`update_releases.py` 重新
打包时只更新 `file` / `size` 两个键，手填的 `url` 不会被抹掉。

**每条记录还有一个 `claude`**：这一版配着哪个版本的 claude 测过的。打包时脚本自己
读打包机上的 `claude --version` 写进去（见「查 claude 新版」），读不到就留空、页面
上不画那一行。所以**不用手填**；手填也行——重打同一个版本时它只补空的，不动已经
写过的。页面上它画成「配 claude 2.1.150 测过」，最新那版的下面也补一句。

## Linux 那半边怎么端到端验

`_probe_nix_ui.py` 整条在 WSL 里跑（被 .gitignore 收着，不进仓库），验的是启动器
在 Linux 上的接线：`HOME` 换成 `/tmp/nixprobe/home` 这个沙箱，**绝不碰真实的
`~/.claude`**；`claude` 换成 PATH 上一个 sleep 着的假脚本（`--version` 会答
`9.9.9 (Claude Code)`），验的不是 claude 本身，是启动器怎么起它、怎么认它、怎么关它。
分六节：界面该藏的东西（没内嵌那一栏、没 conhost 注解）、起会话（pid 当句柄、认出来、
行上是「关掉 / 整理交接文档」而没有「移过来 / 置顶」）、关会话（整组带走，`/proc/<pid>`
没了）、搬迁（靠 `gio trash`，逐条对账 + 旧的真进了 `~/.local/share/Trash`）、
搬迁前该拦的、对话框。

**截图只能从 X 这一侧取，不能从 Windows 那侧截。**

先试的是 Windows 那侧：WSLg 的窗口在桌面上是实打实的 Win32 窗口（类名
`RAIL_WINDOW`），看着截得到。结果 `PrintWindow` 的 0/1/2 三个旗标、以及从窗口 DC
`BitBlt`，**全都只回一张单色图**——远端渲染的窗口不认 `WM_PRINT`，也没有一块能读的
DC。剩下就只有截屏了，但截屏会把用户当时压在那块矩形上的窗口一起拍进来（真试了一次，
拍出来是用户的终端）。这台机器是他自己在用的，这条路不能走。

所以改成在 WSL 里问 X 要：`XGetImage` 拿的是**那个 X 服务器里的像素**，Windows 那侧
的程序压根不在这个 X 服务器里，怎么截都不会拍到。两个坑：

- **取窗口本体的位图，别取根窗口那块矩形。** 根窗口在这台机器的 Xwayland 上直接
  `BadMatch`（`Major opcode 73 (X_GetImage)`），而且 X 错误默认是致命的，探针当场就死。
- **取之前先 `lift()` 再让 Tk 跑几轮 `update()`。** X 不保存被挡住的像素，半掩着的窗口
  `XGetImage` 出来的是一片没定义的糊东西——第一版就横着好几条空白的带子，看着像控件
  没画出来，其实是没露出来。（同一个坑在 Windows 桌面截图那边也提过一次，见「搬运式
  重构」那节。）

WSL 里一件截图工具都没有（`import` / `scrot` / `xwd` / `convert` 全无，也没装 PIL），
所以 PNG 是拿 `zlib` + `struct` 手写的，十几行，够用。

## 还差什么

- **代码签名**：没签名的话下载下来会被 SmartScreen 拦。真要转发给外人，
  这一步影响观感最大。官网「怎么装」里先写了句"点更多信息 → 仍要运行"顶着。
- **只有 64 位**（`ArchitecturesAllowed=x64compatible`），32 位 Windows 装不上。
- **Linux 版发是发出去了，但没在正经桌面上装过**：`build/打包.sh` 出的 tar.gz 已经
  传上 Gitee 和 GitHub 两边的 0.1.0 发行版，官网那格是亮的、点得动。要提醒的是它
  只在 WSL 里验过（`_probe_frozen_nix.py` 那三条），**没在一台真 Linux 桌面的机器上
  解压跑过**——缺个 so、字体不对这类事，WSLg 那套未必照得出来。另外打包机（WSL）
  上没装 claude，所以这版记录里 `claude` 那格是空的。
- **macOS 版**：官网留好位置了，什么时候有包什么时候亮。代码那一侧写是写完了，
  **但手上没有 Mac，一行都没真跑过**——`nixhost.py` 里那两处（`_spawn_macos` 和
  丢回收站）自己就写着这句话，别当成验过的。

## 搬运式重构怎么保证没改坏

拆模块和拆对话框都是纯搬运，但"搬运时少抄一行 import"这类错误，在运行到
那条分支之前不会暴露。这两次用的三道检查，以后大改结构可以照做：

1. **定义集合与源码文本比对。** 用 `ast` 取出重构前的每个函数，跟重构后的
   逐字比。结果应该是"除了有意改的那几处，其余全等"。
   注意**别把类整体算进去**——`Launcher` 换了基类、去掉一半方法，类那段文本
   本来就该变，算进来只会报一条没意义的"文本变了"。
2. **漏 import 扫描。** 反汇编每个模块，把所有 `LOAD_GLOBAL` 的名字揪出来，
   既不在本模块 globals 里、又不是内置的，就是漏了 import。注意要按
   `__module__` 过滤，否则一路 import 进来的函数会按本模块的 globals 去量，
   满屏假报错。
3. **多余 import 扫描。** 反过来说，搬走之后原模块会剩下一堆没用的 import。
   数一下每个 import 进来的名字在文件里出现的次数，只出现一次（就是 import
   那一行）的列出来手工核。这个只能当提示，字符串和注释会造成误判。

外加 A/B 截图：同一套沙箱配置下，重构前后各跑一遍，截屏逐像素比。主窗口和
"加模型"对话框都这么比过。两个坑：
- **要排除窗口阴影那一圈**——它是 DWM 拿桌面背景混合出来的，跟代码无关，
  不排除的话满屏都是 1-2 个像素的假差异。
- **沙箱路径要写死**，别用 `mkdtemp`：临时目录名每次都变，界面上那行路径
  跟着变，逐像素比时全是噪声。

还有一件容易漏的：界面上跑不到的分支（错误提示、异常处理）三道检查都盖不住
——它们只保证"搬运没错"，不保证"逻辑对"。真遇到这类分支，单独写探针去撞它。

## 打包那步怎么验

跟重构一个道理：跑不到的地方就不知道对不对。**打包脚本本身也是"跑不到就不知道"
的东西**——写完不跑一遍，它跟没写一样。所以两边都真跑过：Windows 那条在
`打包.bat` 里，Linux 那条在 WSL 里 `sh build/打包.sh` 跑到底，PyInstaller、tar、
`docs/releases.js` 三步都出来了才作数。

Linux 那份冻出来的东西另有一个探针 `_probe_frozen_nix.py`（同样不进仓库），验三样：

- **冻出来的 `--hook --handoff` 是通的**：塞一段 JSON 进 stdin，退出码 0、没有
  traceback、也没抱怨 display——hook 那条路上不能有 tkinter，冻进去之后尤其要确认。
- **主界面真开得出来**，而且**截一张图**。脚本自己用 `XQueryTree` 在 X 上前后各点
  一次名，多出来那扇就是它；截图还是 `XGetImage` 那一套（见「Linux 那半边怎么
  端到端验」）。这一步才是"tcl/tk 和字体都跟着打包进去了"的证据——进程活着不代表
  界面画得出来。
  找窗口要**递归着走**：WSLg 那边窗管会把客户端塞进一层 frame 窗口里，只问根窗口的
  直接子节点，捞到的是 frame，而且那上面没有标题。
- **配置确实写到沙箱里了**（`$HOME/.claude_tool/launcher.json`），顺带证明没碰真实
  的 `~/.claude`。

Windows 那边验这几样：

- **冻出来的 hook 单独撞一遍。** `echo '{"cwd":"…"}' | dist\claude_tool\claude_tool.exe
  --hook --handoff`，连打几次看节流（前两次放行、第三次拦、第四次重新计数）和
  中文路径；`--hook --auto-continue` 再撞一遍连计数和上限。这是整条链上最容易碎的
  一环，必须单独测，不能靠"主界面能开"推断。
- **A/B 截图验冻结版。** 源码版和冻结版各截一张逐像素比，用来确认 PyInstaller
  没漏掉 tcl/tk、字体或插件。仓库根下的本地脚本 `_probe_ab_shot.py` 干这个
  （被 .gitignore 收着，不进仓库）：它把沙箱 `USERPROFILE` 指到一个写死的路径、
  跑前清干净、按窗口标题找 HWND 挪到固定位置再截。

  **"冻结版命令行到底长什么样"这件事没做端到端验证**——那要真拉一次 claude 会话。
  验的是两头：探针在真冻结进程里读到 `frozen=True`、`sys.executable` 就是 exe 路径，
  而 `hook_command()` 的分叉是单独用假 `frozen` 撞过的。拼起来是确定的，但确实
  没有一次"从界面点出去、读回写下的那行命令"的实跑。要补的话在这里补。

- **装一遍再卸一遍，量三样东西。** 卸载**不能只看安装目录空没空**：
  - 安装目录整棵没了、桌面和开始菜单快捷方式没了、注册表 `Uninstall` 项没了；
  - `~/.claude_tool/` 的文件数**和 `launcher.json` 的 md5** 跟装之前一致
    ——"卸载不删用户数据"是承诺，得量出来，不能凭"Inno 默认不删"推断；
  - 卸载日志里 `Removed all? Yes`。

- **一个踩过的坑：git-bash 会改写 `/` 开头的参数。** `./Setup.exe /VERYSILENT`
  到 Inno 手里变成了 `"C:/Program Files/Git/VERYSILENT"`，静默安装根本没生效，
  于是 `[Run] postinstall` 把程序真拉起来了——程序文件被运行中的自己占着，
  卸载删不掉，日志里报 `Failed to delete the file; it may be in use (5)`。
  这个错看着像"卸载器有 bug"，其实是测试命令错了。传参数用 PowerShell 的
  `Start-Process -ArgumentList ... -Wait`（顺带能拿到真实退出码），或者设
  `MSYS2_ARG_CONV_EXCL='*'`。
